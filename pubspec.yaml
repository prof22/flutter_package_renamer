name: flutter_package_renamer
description: A command-line tool to rename Android package names and iOS bundle IDs with undo support.
version: 1.0.0
homepage: https://github.com/prof22/flutter_package_renamer

environment:
  sdk: ^3.11.0

# Add regular dependencies here.
dependencies:
  path: ^1.9.0
  yaml: ^3.1.3
  args: ^2.5.0

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.6

executables:
  flutter_package_renamer: flutter_package_renamer